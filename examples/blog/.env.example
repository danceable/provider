# Copy to .env (or export) to override the built-in defaults.
# These defaults let `go run .` work against a local MongoDB on the default port.

BLOG_HTTP_ADDR=:8080
BLOG_MONGO_URI=mongodb://localhost:27017
BLOG_MONGO_DB=blog
BLOG_PER_PAGE=5
